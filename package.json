{
  "name": "cf-workers-template",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers project template with Hono",
  "type": "module",
  "scripts": {
    "check": "wrangler types && tsc --noEmit && tsc -p test/tsconfig.json --noEmit && vitest run",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler d1 migrations apply myapp --remote && wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc -p test/tsconfig.json --noEmit",
    "types": "wrangler types"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lsongdev/cf-workers-reader.git"
  },
  "license": "MIT",
  "dependencies": {
    "hono": "^4.11.3",
    "jose": "^6.2.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  },
  "packageManager": "pnpm@10.30.3"
}
